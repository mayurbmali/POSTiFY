distlib==0.3.8
filelock==3.15.4
packaging==24.2
platformdirs==4.2.2
virtualenv==20.26.3
Django==5.0.7
django-crispy-forms>=1.14.0  # For enhanced form rendering
Pillow>=9.0  # For handling image uploads if needed
sqlite3  # If using SQLite, this is built-in for Python and doesn't need installation
gunicorn>=21.2  # For deployment (optional)
